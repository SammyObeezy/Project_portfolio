import _ from 'lodash';
export default {
    onSelect(state = { selectedIndices: [] }, index) {
        const currentIndices = _.get(state, 'selectedIndices', []);
        return {
            ...state,
            selectedIndices: _.xor(currentIndices, [index]),
        };
    },
};
//# sourceMappingURL=ButtonGroup.reducers.js.map